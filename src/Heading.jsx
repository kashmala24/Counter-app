export function Heading(){
    return <h1 className="title">Fancy Counter</h1>
}

// instead of writing export detail at the end, write the before the function

// linecss way
{/* <h1 style={{
        color: "red",
        backgroundColor: "grey"}
    }>Fancy Counter</h1> */}